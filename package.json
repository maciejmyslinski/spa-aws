{
  "name": "spa-aws",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@netguru/react-scripts": "3.0.0",
    "normalize.css": "^8.0.0",
    "react": "^16.4.1",
    "react-dom": "^16.4.1",
    "react-hot-loader": "^4.3.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "stylelint": "stylelint \"src/**/*.scss\"",
    "eslint": "eslint \"src/**/*.js\"",
    "cloudformation": "aws cloudformation deploy --stack-name spa-aws --template-file ./cloudformation.json",
    "deploy": "aws s3 sync ./build s3://spa-aws"
  }
}
